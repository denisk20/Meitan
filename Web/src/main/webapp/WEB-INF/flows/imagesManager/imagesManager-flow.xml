<?xml version="1.0" encoding="UTF-8"?>
<flow xmlns="http://www.springframework.org/schema/webflow"
	  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	  xsi:schemaLocation="http://www.springframework.org/schema/webflow http://www.springframework.org/schema/webflow/spring-webflow-2.0.xsd"
	  parent="global">

	<persistence-context/>

	<input name="imageAware" required="true"/>
	<!--todo-->
	<!--<input name="imagesDao" required="true"/>-->

	<view-state id="imagesManager">
		<on-entry>
			<evaluate expression="utils.asList(imageAware.getImages())" result="viewScope.images" result-type="dataModel"/>
		</on-entry>
		<transition on="upload" to="imagesManager">
			<evaluate expression="fileUploadHandler.sayHello()"/>
			<evaluate expression="imageIdGenerationService.generateIdForNextImage(imageAware)" result="requestScope.imageName"/>
			<evaluate expression="fileUploadHandler.precessTempFile1(imageAware, flowRequestContext, imageName)" result="requestScope.image"/>
			<evaluate expression="imageAware.addImage(image)"/>
			<evaluate expression="flowScope.tempImagesService.addToPersistQueue(image)"/>
			<evaluate expression="flowScope.tempImagesService.removeFromDiscardQueue(image)"/>
<!--
			<evaluate expression="imageDao.makePersistent(image)"/>
			<evaluate expression="categoryDao.merge(imageAware)"/>
-->
		</transition>

		<transition on="delete" to="imagesManager">
			<evaluate expression="imageAware.removeImage(images.selectedRow)"/>
			<!--todo how about deleting persistent instances?-->
			<evaluate expression="flowScope.tempImagesService.addToDiscardQueue(images.selectedRow)"/>
			<!--<evaluate expression="flowScope.tempImagesService.removeFromPersistQueue(image)"/>-->
			<!--<evaluate expression="imageDao.deleteFromDisk(images.selectedRow)"/>-->
		</transition>

		<transition on="save" to="save"/>
	</view-state>

	<end-state id="save"/>
	<end-state id="cancel"/>
</flow>